{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "run dev with nvm",
      "type": "shell",
      "command": "zsh",
      "args": [
        "-c",
        "export NVM_DIR=\"$HOME/.nvm\" && source \"$NVM_DIR/nvm.sh\" && nvm use && npm run dev"
      ],
      "problemMatcher": [],
      "detail": "next dev with nvm and .nvmrc"
    }
  ]
}
